/**
 * Upgrade dialog stub — NeuroDual Lite has no premium tier,
 * so this renders nothing. Keeps call-sites intact.
 */
export function UpgradeDialog(_props: {
  isOpen: boolean;
  onClose: () => void;
  source?: string;
}): null {
  return null;
}
