/**
 * Rewards Module
 *
 * XP-based Premium rewards (Train-to-Own system).
 */

export {
  rewardAdapter,
  createRewardAdapter,
  noopRewardAdapter,
  initRewardAdapter,
  resetRewardAdapter,
} from './reward-adapter';
