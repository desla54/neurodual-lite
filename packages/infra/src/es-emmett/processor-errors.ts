/**
 * Stub — processor error recording (no longer needed).
 */
export async function recordProcessorError(
  _db: unknown,
  _args: { processorName: string; error: unknown },
): Promise<void> {
  // No-op
}
